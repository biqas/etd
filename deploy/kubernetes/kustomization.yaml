namespace: etd-dev

resources:
- namespace.yaml
- etd-web
- basicauth.yaml
- ingress.yaml         # etd.it-blue.net (Dev, BasicAuth)
- ingress-prod.yaml    # elektrotechnikdesch.de + www (Prod, kein BasicAuth) — wartet auf DNS-Cutover

generatorOptions:
  disableNameSuffixHash: true
